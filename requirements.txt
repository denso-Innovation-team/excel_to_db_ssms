# Excel to SQL Server - Fixed Requirements
# แก้ไขปัญหา pymssql compilation error

pandas>=2.0.0
sqlalchemy>=2.0.0
openpyxl>=3.1.0
pyodbc>=4.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pydantic>=2.0.0

# Note: pymssql removed due to Python 3.13 compatibility issues
# จะใช้เฉพาะ pyodbc สำหรับ SQL Server connection